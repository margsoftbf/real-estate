import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  // Enable webpack for Turbopack compatibility
  turbo: {},
};

export default nextConfig;
